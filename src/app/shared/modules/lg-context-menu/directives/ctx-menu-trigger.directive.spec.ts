import { CtxMenuTriggerDirective } from './ctx-menu-trigger.directive';

describe('CtxMenuTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new CtxMenuTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
